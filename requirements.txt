# Runtime dependencies, pinned to a set verified to work together on
# Python 3.10-3.12.
#
# Tkinter is NOT listed here. It ships with CPython and is not installable
# from PyPI; the line "tkinter" used to sit in this file and made
# `pip install -r requirements.txt` fail for everyone who cloned the repo.
# On Debian/Ubuntu, install the system package instead:  sudo apt install python3-tk
#
# For a looser, range-based install, use the package itself: pip install .

pandas==2.2.3
matplotlib==3.10.1
seaborn==0.13.2
# Matplotlib 3.10.1 uses aliases deprecated by pyparsing 3.3.
pyparsing==3.2.3
